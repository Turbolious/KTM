{
    // Time in seconds between automatic garbage collection runs
    garbage_collection_interval = 30

    // List of substrings; any texture containing these will be ignored (one per line)
    blacklist = icon
    blacklist = flag
    blacklist = kerbalkonsole

    // Comma-separated list of suffixes to identify normal maps
    normal_map_suffixes = _bump, _nrm
}
